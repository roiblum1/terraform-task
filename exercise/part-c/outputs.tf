# TODO: output "vm_names" — a list of every VM's name.
# module.vm is now a list (because of count), so this needs a `for`
# expression, not a plain module.vm.name like in Part B2. See
# CHEATSHEET.md for the [for x in list : ...] syntax.
