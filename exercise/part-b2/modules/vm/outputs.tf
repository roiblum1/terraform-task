# TODO: output "name"       -> vsphere_virtual_machine.vm.name
# TODO: output "id"         -> vsphere_virtual_machine.vm.id
# TODO: output "ip_address" -> vsphere_virtual_machine.vm.default_ip_address
#
# These three are the module's entire interface to the outside world — the
# root module can only see what you output here.
